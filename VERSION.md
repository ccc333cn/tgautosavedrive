# TGAutoSaveDrive 版本记录

当前版本：`v0.0.24`

发布日期：2026-06-11

## Docker 镜像

- `ccc333i/tgautosavedrive:v0.0.24`
- `ccc333i/tgautosavedrive:latest`

---

## v0.0.24 — 2026-06-11

### 新增

- 新增 123 云盘支持：网盘管理、资源转存、追更任务全链路适配（仅适配 @regeng123 频道资源）。
- 123 云盘追更支持时间标记文件（从根目录复制并重命名）。
- 订阅功能适配 123 云盘，支持先到先得/仅115/仅123 三种模式。

### 修复

- 修复追更任务卡片 LastEpisode 显示随机集数的问题（现在始终显示最大集号）。
- 修复 123 网盘手动转存剧集时不应自动创建文件夹的问题（仅电影创建）。

---

## v0.0.23 — 2026-06-04

### 修复

- 修复 115 Cookie 失效时追更任务未推送通知（PushPlus / TG Bot）的问题。
- 修复 Cookie 失效导致任务被标记为异常后不再自动执行的问题（Cookie 是全局问题，不应改变任务状态，任务保持活跃等待下次 Cron 重试）。
- 修复订阅电影转存时 Cookie 失效同样未推送通知的问题。

---

## v0.0.21 — 2026-05-24

### 修复

- 修复首次启用 Emby 媒体库匹配时豆瓣榜单无 Emby 刷新按钮和已入库标注的问题（emby_available 判断改为读取设置而非缓存状态，缓存为空时自动触发后台刷新）。
- 修复订阅电影（一次性转存）完成后未触发 SmartStrm Webhook 和 Emby 媒体库扫描通知的问题。
- 消除追更任务去重查询产生的 GORM `record not found` 日志噪音。

### 优化

- 移动端近 7 天趋势图日期标签改为只显示日数（跨月时显示 M/D），避免文字重叠。
- 移动端豆瓣影片详情页操作按钮自动换行，不再溢出被截断。
- 追更任务卡片和批量操作按钮的 emoji 字符（▶ ▷ ⏸）全部替换为 Lucide SVG 图标。
- 转存概览「订阅中」统计卡片补充紫色底部彩条，与其他卡片风格统一。
- 移除豆瓣榜单 Emby 按钮旁的「共 X 部 · Emby Y」统计文字。
- 更新内容弹窗「知道了」按钮居中对齐。

---

## v0.0.20 — 2026-05-22

### 新增

- 豆瓣榜单 × Emby 媒体库联动：浏览豆瓣榜单时自动标注 Emby 中已有的影片（绿色「已入库」角标）。
- Emby 设置新增「媒体库匹配」独立开关，与 Emby 扫描通知互不影响。
- Emby 库存标题持久化到 SQLite，启动时秒加载，转存后自动刷新，豆瓣页面支持手动刷新。
- 添加频道时支持粘贴完整 Telegram 链接（如 `https://t.me/xxx`），自动提取频道用户名。
- Emby 地址未加 `http://` 前缀时自动补全，避免连接失败。

### 修复

- 修复频道名包含斜杠（如误粘贴完整链接）时无法删除的问题。

---

## v0.0.19 — 2026-05-14

### 新增

- 订阅新增画质格式过滤：支持指定 HDR 或 DV（杜比视界），精准匹配所需画质版本。

---

## v0.0.18 — 2026-05-11

### 优化

- 搜索结果实时性：频道缓存增量扫描改为同步执行，首次搜索即可显示频道最新资源。

### 界面

- 时间标记设置移至 SmartStrm Webhook 下方，逻辑分组更清晰。
- 时间标记描述增加说明：追更任务完结时会自动删除对应的时间标记文件。

---

## v0.0.17 — 2026-05-10

### 新增内容

- 完结剧集一次性转存：带「完结」标签的剧集与电影一样，自动选画质最高版本直接转存，不再创建追更任务。
- 订阅弹窗新增「搜索封面」按钮：手动新建订阅时可一键从豆瓣搜索并填充封面图。
- 追更任务自动获取封面：订阅匹配创建追更任务时，若无封面则自动从豆瓣获取并下载到本地。
- 订阅创建追更任务后自动触发首次追更执行。
- 追更任务显示标题自动从资源解析年份（如「亢奋 (2019)」）。

### 修复内容

- 修复订阅匹配不过滤年份的问题（同名不同年份资源误匹配）。
- 修复订阅有完结和连载资源时，连载被优先匹配的问题（现在完结版优先）。
- 修复追更任务季过滤逻辑漏洞（未解析出季号的资源不被过滤，导致 S01/S02 混存）。
- 修复订阅未指定季时追更任务不设置季文件夹的问题（现在自动检测最新季）。
- 修复豆瓣详情页点「订阅」时自动填入不准确年份的问题。

---

## v0.0.16 — 2026-05-08

### 新增内容

- 新增订阅任务功能：支持从豆瓣榜单一键订阅，定时自动检索频道并转存。
- 订阅支持电影（一次性转存）和剧集/综艺/动漫（创建追更任务）两种模式。
- 电影订阅匹配多个结果时，自动选择画质最高的资源（DV HDR10 > DV > HDR10）。
- 订阅任务支持编辑（标题、类型、画质、检查频率、过期天数）。
- 新增 Telegram 交流群入口。
- 追更任务、订阅任务新增「纪录片」类型，覆盖电影/综艺/动漫/电视剧/纪录片全部分类。
- SmartStrm 任务名映射支持逗号分隔多任务名，转存时逐个发送 webhook。
- 通知开关（SmartStrm / Emby / PushPlus / TG Bot / 时间标记）勾选/取消后自动保存。

### 修复内容

- 修复今日转存统计使用 SQLite UTC 时区导致不准的问题。
- 修复 Vue3 模板中下划线前缀变量名导致的运行时错误。
- 修复频道配置解析不兼容对象数组格式的问题。
- 修复 SmartStrm 启用后无法关闭的问题（取消勾选后保存按钮消失）。
- 修复从搜索结果快速创建追更任务时封面图丢失的问题。
- 修复追更任务转存后未触发 SmartStrm/Emby 通知的问题（未配置推送通道时入队被跳过）。
- 修复 Telegram MTProto 不支持 HTTP 代理的问题（新增 HTTP CONNECT 隧道支持）。

### 调整内容

- SmartStrm 映射固定为电影/综艺/动漫/电视剧/纪录片 5 种类型，移除自定义添加/删除。
- 导航栏重排：订阅任务移至追更任务下方，系统日志移至系统设置上方。
- 转存概览移除「已暂停」卡片。
- 订阅卡片频道信息对齐追更任务样式（Telegram 图标 + 频道标签）。
- 转存日志更名为系统日志。

---

## v0.0.15 — 2026-05-07

### 新增内容

- SmartStrm 任务名映射改为动态列表，新增「动漫」内置类型，支持自定义新增/删除任意类型映射。

### 修复内容

- 修复 Telegram API 发送验证码超时无响应的问题（MTProto 连接未走代理）。

---

## v0.0.14 — 2026-05-04

### 修复内容

- 修复代理设置无法保存的问题（saveSettings 遗漏代理字段）。
- 修复代理设置保存后刷新页面不回显的问题（loadSettings 遗漏代理字段）。
- 修复资源搜索、缓存扫描、图片代理、TG Bot 推送不走代理的问题，所有 Telegram 相关 HTTP 请求现在统一使用代理配置。

---

## v0.0.13 — 2026-04-30

### 新增内容

- 侧边栏左下角新增 GitHub 项目入口，方便用户直接打开公开仓库。
- 新增 `/api/version` 版本检测接口，自动读取 Docker Hub 版本标签并缓存结果。
- 网页左下角新增当前版本展示；当 Docker Hub 有新版本时显示红色新版本标签。

### 修复内容

- 修复更换浏览器首次打开仍重复出现新手引导页的问题。
- 修复基础设置中 Cron 表达式保存时触发浏览器保存密码提示的问题。
- 修复配置页 PushPlus、Telegram Bot、Emby 等敏感输入可能被浏览器误判为密码的问题。
- 修复 Telegram API 配置保存按钮没有调用专用保存逻辑的问题。
- 修复侧边栏 GitHub 图标未显示的问题，改为使用本地 SVG 资源。
- 调整侧边栏底部「退出登录」按钮居中显示。
- 修复刷新页面时偶发显示 `{{...}}` 模板文本的问题，前端关键依赖改为本地静态资源并在 Vue 挂载前隐藏模板。

### 调整内容

- iPhone 添加到主屏幕时显示名称改为「TG追更助手」。
- 版本发布流程补充页面版本常量同步规则，避免网页与后端版本不一致。

---

## v0.0.12 — 2026-04-30

### 修复内容

- 优化敏感信息显示按钮，在深色背景下使用浅色图标，避免图标不可见。
- 修复 Telegram API ID / API Hash 输入框触发浏览器保存密码提示的问题。
- 修复 HTTP 缓存更新后频道管理页更新时间、有效新增数量不准确的问题。
- 修复资源搜索、追更任务和缓存定时更新触发的缓存写入不会同步刷新频道缓存状态的问题。
- 修复不可访问或未公开频道被提示为「扫描完成，共缓存 0 条资源」的问题。

### 调整内容

- 明确 Telegram API 模式为即时搜索，不再依赖或维护 HTTP 频道缓存。
- 转存概览「下次调度」纳入缓存定时更新，并只显示下一次最早要执行的事项。
- 系统设置「缓存定时更新」补充说明：定时任务固定在整点的 39 分触发。

---

## v0.0.11 — 2026-04-30

### 修复内容

- 修复未配置 115 Cookie 时，点击「网盘管理」会被误判为系统登录失效并跳回登录页的问题。
- 调整 115 Cookie 未配置或失效时的接口返回，让前端在当前页面显示配置错误提示。
- 收紧前端自动退出登录判断，仅在系统登录凭证真正失效时退出，避免业务接口错误误触发退出。

---

## v0.0.1 — 2026-04-29

首个完整版本，Go 重构全部功能实现。

### 核心功能

- Telegram 频道搜索：HTTP 抓取 + MTProto 双模式，多频道并行搜索，全量/增量扫描，本地缓存。
- 115 网盘集成：扫码登录、文件浏览、资源转存、云下载。
- 自动追更：Cron 定时任务，按剧名/季自动整理目录结构，去重转存，超期自动完结。
- 智能解析：支持 Case A/B/C/D 四种频道格式，自动提取元数据并按画质保留最优资源。

### 联动推送

- SmartStrm：转存后触发 Webhook 生成 STRM 文件，按类型去重。
- Emby：SmartStrm 通知后延时触发媒体库扫描。
- 通知：PushPlus 微信 + Telegram Bot 双通道，30s 防抖合并推送。
- 任务完结推送：过期自动完结时推送通知。

### 前端与系统

- Vue 3 单文件 SPA，Obsidian Dark 主题，Lucide Icons SVG 图标系统。
- 转存概览、资源搜索、网盘浏览器、频道管理、豆瓣榜单、日志、记录、系统设置等页面。
- 数据备份恢复、日志自动清理、缓存自动刷新、Bearer Token 认证、PWA 支持。
